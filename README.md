# atmpin
Python ATM simulation that checks PIN, displays balance, and processes withdrawals. Validates entered amount, updates balance if sufficient, and shows error messages for incorrect PIN or low balance. Simple example of conditional logic and user input handling.
